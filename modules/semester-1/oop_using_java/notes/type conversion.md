some of type can be automatically coverted to other types

but some of them needs explicit converion


(type) variable name;

byte s;

int x = 10;

s = (byte) x;

here the integer x is converted to byte using explicit conversion

